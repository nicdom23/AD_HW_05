#include "heap_sort.h"
//#include "binheap.h"

//#include </usr/lib/libbinheap.so>
void heap_sort(void *A, const unsigned int n, 
               const size_t elem_size, 
               total_order leq)
{
    //binheap_type *heapA=build_heap(A,n,n,elem_size,leq);//build maxheap

}